# 原始资料采集规则

1. 所有新材料先进入 `raw/`，不得直接发布为可信知识。
2. 导入时保留原文件名、媒体类型、采集时间、来源类型和内容哈希。
3. 网页、聊天、会议或人工摘录需要在正文中写明可核实的出处；不能确认出处时明确标记。
4. 原始正文或二进制内容不得静默修改。修正内容应重新采集为新的 raw ID。
5. 默认禁止导入私钥、凭据、token、`.env` 和密码数据库。
6. 一个 raw 条目表达一次采集事实；目录批量导入仍为每个文件分配独立 ID。
